description "Aptly API Service"
author "wilrnh"

start on started mountall
stop on shutdown

console <%= @log %>

setuid <%= @user %>
setgid <%= @group %>

exec /usr/bin/aptly api serve -listen=<%= @listen %>
